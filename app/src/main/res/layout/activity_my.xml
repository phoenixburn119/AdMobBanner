<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:id="@+id/Background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="@dimen/activity_vertical_margin"
    tools:context=".MyActivity"
    tools:ignore="MergeRootFrame">

    <com.google.android.gms.ads.AdView
        android:id="@+id/ad_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        ads:adSize="BANNER"
        ads:adUnitId="@string/banner_ad_unit_id" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Change Color"
        android:id="@+id/DoButton"
        android:layout_below="@+id/TimeClock"
        android:layout_centerHorizontal="true"/>

    <TextClock
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/TimeClock"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/DoButton"
        android:layout_alignEnd="@+id/DoButton"
        android:layout_alignLeft="@+id/DoButton"
        android:layout_alignStart="@+id/DoButton"
        android:textSize="25dp"
        android:textStyle="bold"
        android:textColor="#33ff0f"
        android:background="#000000"/>

</RelativeLayout>
